export const selectUploadedImage = (state) => state.uploads.image;

export const selectUploadLoading = (state) => state.uploads.loading;

export const selectUploadError = (state) => state.uploads.error;